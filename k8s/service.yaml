apiVersion: v1
kind: Service
metadata:
  name: triage-service
spec:
  type: NodePort
  selector:
    app: triage-app
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30007